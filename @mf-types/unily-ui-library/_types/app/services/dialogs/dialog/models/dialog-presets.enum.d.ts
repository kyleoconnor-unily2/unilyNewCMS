/**
 * Represents a combination of preset styles that can be applied to a dialog.
 */
export declare enum DialogPresets {
    /**
     * No class presets.
     */
    NONE = 0,
    /**
     * Applies class presets for a narrow dialog.
     */
    NARROW = 1,
    /**
     * Applies class presets for a wide dialog.
     */
    WIDE = 2,
    /**
     * Applies class presets for a full screen dialog.
     */
    FULL_SCREEN = 4,
    /**
     * Applies class presets for a dialog with padding at the bottom.
     */
    BODY_BOTTOM_PAD = 8,
    /**
    * Applies class presets for a overflow styles.
    */
    OVERFLOW = 16
}
