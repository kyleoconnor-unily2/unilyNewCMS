import { ToastService } from '../../services/toasts/toast/toast.service';
/**
 * Make the ToastV2Service service temporarily extend the existing ToastService service to reuse its functionality until the migration is complete.
 */
export declare class ToastV2Service extends ToastService {
}
