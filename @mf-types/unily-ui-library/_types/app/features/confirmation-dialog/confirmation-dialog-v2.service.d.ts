import { ConfirmationDialogService } from '../../services/dialogs/confirmation-dialog/confirmation-dialog.service';
/**
 * Make the ConfirmationDialogV2Service service temporarily extend the existing ConfirmationDialogService service to reuse its functionality until the migration is complete.
 */
export declare class ConfirmationDialogV2Service extends ConfirmationDialogService {
}
